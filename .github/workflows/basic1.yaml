name: Basic Github Action pipeline

on:
  #workflow_dispatch: 
 # push:
 #   branches: ["master"]
 # pull_request: 
 #   branches: ["master"]

jobs:
  Basics:
    runs-on: [self-hosted, linux]

    steps:
      - name: write into a file
        run: echo "hello from Github action pipeline"
      
